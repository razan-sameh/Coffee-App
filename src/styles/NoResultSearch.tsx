import { StyleSheet } from "react-native";
import { heightScale, mbdlScreenHeight, mdblBAR_HEIGHT, moderateScale, strINTER_MEDIUM500_Font, strINTER_SEMIBOLD600_Font, strInpitBtnColor, strPrimaryColor, strSecondColor, widthScale } from "./responsive";

export const Styles = StyleSheet.create({
    wall:{
        width:widthScale(375),
        backgroundColor:strSecondColor,
        height: mbdlScreenHeight 
    },
    backArrowContainer:{
        
    }
})